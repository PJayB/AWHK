<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:HotKeyCustomControlLibrary">
    <Style TargetType="{x:Type local:HotKeyBox}">
        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
        <Setter Property="KeyCombo" Value="{x:Null}"/>
        <Setter Property="DisabledModifiers" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HotKeyBox}">
                    <TextBox x:Name="txtDisplayBox"  AutoWordSelection="True"
                             IsReadOnly="True" IsReadOnlyCaretVisible="True"
                             VerticalContentAlignment="{Binding RelativeSource={RelativeSource AncestorType=local:HotKeyBox, Mode=FindAncestor}, Path=VerticalContentAlignment}"
                             HorizontalContentAlignment="{Binding RelativeSource={RelativeSource AncestorType=local:HotKeyBox, Mode=FindAncestor}, Path=HorizontalContentAlignment}"
                             FontFamily="Segoe UI Symbol"
                             Text=""/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
